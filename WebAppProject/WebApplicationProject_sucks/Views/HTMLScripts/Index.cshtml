@model IEnumerable<WebApplicationProject_sucks.HTMLScript>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Content)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Content)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.HTMLScriptID }) |
            @Html.ActionLink("Details", "Details", new { id=item.HTMLScriptID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.HTMLScriptID })
        </td>
    </tr>
}

</table>
