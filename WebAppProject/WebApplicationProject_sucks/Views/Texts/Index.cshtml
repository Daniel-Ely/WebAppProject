@model IEnumerable<WebApplicationProject_sucks.Text>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model._Text)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item._Text)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TextID }) |
            @Html.ActionLink("Details", "Details", new { id=item.TextID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TextID })
        </td>
    </tr>
}

</table>
