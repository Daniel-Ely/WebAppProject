@model WebApplicationProject_sucks.Models.User
@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Home</title>
    <link rel="stylesheet" href="~/Content/Home.css?ver=<?php echo rand(111,999)?" type="text/css">
</head>
<body>

    <!--here we place the content that differs-->


    <div class="row">

        <div class="side">
            <h2>About us</h2>
            <h5>Photo of me:</h5>
            <div class="fakeimg" style="height:200px;">Image</div>
            <p>Some text about me in culpa qui officia deserunt mollit anim..</p>
            <h3>More Text</h3>
            <p>Lorem ipsum dolor sit ame.</p>
            <div class="fakeimg" style="height:60px;">Image</div><br>
            <div class="fakeimg" style="height:60px;">Image</div><br>
            <div class="fakeimg" style="height:60px;">Image</div>
        </div>

        <div class="main">
            <h2>TITLE HEADING</h2>
            <h5>Title description, Dec 7, 2017</h5>

            <br>

            @{
                MyDB db = new MyDB();
                var user = db.Users.Find(Session["UserName"]);
                int postNum = 0;
                var Intrest = user.Interests.OrderBy(i => i.LastTouched).ThenBy(i => i.NumOfVisits);
                foreach (var item in Intrest)
                {
                    var Qroom = db.QuestionRooms.OrderBy(q=>q.DatePublished).Where(q => ((q.Categories.Where(c => c.CategoryName == item.CategoryName)).Count() > 0)).ToList();
                    foreach (var iteam2 in Qroom)
                    {
                        <img src="~/src/QutionRoomOwl.png" alt="Avatar" class="avatar" align="left">
                        <div class="postAndQroom">
                            <h3>@Html.Raw(iteam2.Title)</h3>
                            <h5>publish date , @iteam2.DatePublished.Month  @iteam2.DatePublished.Day,  @iteam2.DatePublished.Year</h5>
                            <p>@Html.Raw(iteam2.Title)</p>
                        </div>

                    }
                }
            }
        </div>
        </div>



    <div class="footer">
        <h2>Footer</h2>
    </div>
    <h1>welcome to your begining page</h1>

</body>
</html>